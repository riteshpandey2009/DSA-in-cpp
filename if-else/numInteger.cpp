#include <iostream>
using namespace std;
int main()
{
    float n;
    cout << "Enter the real number : ";
    cin >> n;
    int y = (int)n;
    if (n = y)
    cout << "Number is integer";
    else
    cout << "number is not an integer";
}